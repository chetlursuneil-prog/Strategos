# STRATEGOS Frontend (placeholder)

This folder will contain the Next.js (App Router) TypeScript frontend for STRATEGOS.

Notes:
- Tech stack: Next.js (App Router), TypeScript, TailwindCSS (planned).
- I will ask which model you want to use before building the production website or integrating any AI model.

To install and run locally (when ready):

```powershell
cd frontend
npm install
npm run dev
```
